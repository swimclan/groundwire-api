image: node:latest

stages:
  - build
  - deploy

build:
  stage: build
  script:
    - npm install express -g
    - npm install
  tags:
    - trader-api

deploy:
  stage: deploy
  script:
    - ./bin/www
  tags:
    - trader-api
